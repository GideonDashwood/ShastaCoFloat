---
phase: 02-encoding-fix-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - sw.js
autonomous: true
requirements:
  - ENC-01
  - ENC-02
  - ENC-03
  - DEPLOY-01

must_haves:
  truths:
    - "Live ride screen shows → (arrow), ° (degree), — (em dash), … (ellipsis) as correct glyphs"
    - "Ride detail view shows · (middle dot) as a correct interpunct separator, not garbled bytes"
    - "Board picker displays emoji icons correctly (orange circles for Onewheel models, blue book for EUC)"
    - "PowerShell scan of index.html returns zero matches for non-ASCII bytes"
    - "Returning users receive the fixed build automatically (sw.js cache version is ridelogger-v22)"
  artifacts:
    - path: "index.html"
      provides: "All non-ASCII bytes replaced with HTML entities or Unicode escapes"
      contains: "no bytes above 0x7F"
    - path: "sw.js"
      provides: "Updated cache version so service worker invalidates old cached index.html"
      contains: "ridelogger-v22"
  key_links:
    - from: "sw.js line 1"
      to: "browser cache"
      via: "CACHE constant version string"
      pattern: "ridelogger-v22"
    - from: "index.html"
      to: "live-temp display"
      via: "boardTempF.toFixed(0) + '&#x00B0;'"
      pattern: "live-temp"
---

<objective>
Replace every garbled non-ASCII byte sequence in index.html with the correct HTML entity or
JavaScript Unicode escape, then bump the service worker cache version so users receive the fix
automatically on their next visit.

Purpose: The app currently shows sequences like `â†'`, `Â°`, `ðŸŸ ` where correct characters should
appear. These are UTF-8 bytes misread as Latin-1. After this plan the source file will contain
only ASCII bytes, and the correct glyphs will render in every browser.

Output:
- index.html with all non-ASCII bytes replaced (ENC-01, ENC-02, ENC-03)
- sw.js with cache version bumped to ridelogger-v22 (DEPLOY-01)
- Commit pushed to origin/main
</objective>

<execution_context>
@C:/Users/bills/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/bills/ShastaCoFloat/.planning/PROJECT.md
@C:/Users/bills/ShastaCoFloat/.planning/ROADMAP.md
@C:/Users/bills/ShastaCoFloat/.planning/STATE.md
@C:/Users/bills/ShastaCoFloat/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all non-ASCII byte sequences in index.html</name>

  <read_first>
    - C:/Users/bills/ShastaCoFloat/index.html (read the ENTIRE file before touching anything — you
      need current line numbers; do NOT assume they match the plan table below exactly)
    - C:/Users/bills/ShastaCoFloat/CLAUDE.md (review bump-SW and commit conventions)
  </read_first>

  <files>index.html</files>

  <action>
    Run the PowerShell scan first to get the exact current line numbers:

      Select-String -Path index.html -Pattern '[^\x00-\x7F]' | Select-Object LineNumber, Line

    Then open index.html with the Write tool (after reading the full file) and apply EVERY
    replacement listed in the table below. The table was built from the scan at planning time;
    if the scan shows additional lines not in the table, fix those too using the same mapping rules.

    REPLACEMENT TABLE — apply all of these:

    Context category A — JavaScript string literals and template literals
    (use \uXXXX Unicode escape so the JS string contains the correct character at runtime):

      Line ~81  comment:  "RPM â†' speed"
                fix:      "RPM → speed"

      Line ~438 comment:  "f3fe â€" board sends challenge"
                fix:      "f3fe — board sends challenge"

      Line ~448 comment:  "f318 (board serial/identifier) â€" used in some"
                fix:      "f318 (board serial/identifier) — used in some"

      Line ~466 comment:  "candidates in order â€" first accepted"
                fix:      "candidates in order — first accepted"

      Line ~477 JS string: "label + ' â†' ['"
                fix:        "label + ' → ['"

      Line ~507 comment:  "First poll after 1.5s â€" gives"
                fix:      "First poll after 1.5s — gives"

      Line ~525 template: "notify failed â€" ${e.message}"
                fix:      "notify failed — ${e.message}"

      Line ~532 template: "read â†' [${Array.from"
                fix:      "read → [${Array.from"

      Line ~535 template: "read failed â€" ${e.message}"
                fix:      "read failed — ${e.message}"

      Line ~554 template: "NOT FOUND â€" ${e.message}"
                fix:      "NOT FOUND — ${e.message}"

      Line ~571 template: "batt bytes=[...] â†' ${boardBattery}%"
                fix:      "batt bytes=[...] → ${boardBattery}%"

      Line ~579 template: "duty bytes=[...] â†' ${boardDutyCycle"
                fix:      "duty bytes=[...] → ${boardDutyCycle"

      Line ~587 template: "temp bytes=[...] â†' ${boardTempF"
                fix:      "temp bytes=[...] → ${boardTempF"

      Line ~587 template: "${boardTempF.toFixed(1)}Â°F"
                fix:      "${boardTempF.toFixed(1)}°F"

      Line ~588 JS string: "boardTempF.toFixed(0) + 'Â°'"
                fix:       "boardTempF.toFixed(0) + '°'"

      Line ~630 JS string: "'Connectingâ€¦'"
                fix:       "'Connecting…'"

      Line ~646 JS string: "'GPS ready â€" tap Start Ride'"
                fix:       "'GPS ready — tap Start Ride'"

      Line ~691 comment:  "Elevation tracking â€" ignore"
                fix:      "Elevation tracking — ignore"

      Line ~1502 comment: "BLE connect â€" only shown"
                fix:      "BLE connect — only shown"

      Line ~1539 comment: "Elevation row â€" always shown"
                fix:      "Elevation row — always shown"

      Line ~1551 comment: "BLE telemetry row â€" only shown"
                fix:      "BLE telemetry row — only shown"

      Line ~1707 JS string: "Reload to pick up new HTML â€" skip"
                fix:        "Reload to pick up new HTML — skip"

      Line ~1709 JS string: "'App update ready â€" will apply'"
                fix:        "'App update ready — will apply'"

    Context category B — HTML text content inside elements
    (use HTML entities so the browser renders the correct glyph):

      Line ~105 JS (textContent assignment): theme === 'dark' ? 'â˜€ï¸' : 'ðŸŒ™'
                This is JS setting textContent, so use Unicode escapes:
                fix: theme === 'dark' ? '☀️' : '🌙'

      Line ~267 template string (injected as innerHTML): "${date} Â· ${time}"
                fix: "${date} · ${time}"

      Line ~314 template string (injected as innerHTML): "${date} Â· <span"
                fix: "${date} · <span"

      Line ~1373 HTML:    <span ...>v20</span>
                This is a static version label in the header — update to v21 (already done
                in Phase 1) or v22 if it still reads v20; check what is actually there and
                match the deployed sw.js version. Do not touch if already correct.

      Line ~1377 HTML:    <span id="theme-icon">â˜€ï¸</span>
                fix: <span id="theme-icon">&#x2600;&#xFE0F;</span>

      Line ~1379 HTML:    <button ...>â»</button>
                fix: <button ...>&#x267B;</button>

      Line ~1386 HTML:    <span class="nav-icon">âš¡</span>
                fix: <span class="nav-icon">&#x26A1;</span>

      Line ~1390 HTML:    <span class="nav-icon rec-dot">â—</span>
                fix: <span class="nav-icon rec-dot">&#x25CF;</span>

      Line ~1394 HTML:    <span class="nav-icon">âœï¸</span>
                fix: <span class="nav-icon">&#x270F;&#xFE0F;</span>

      Line ~1398 HTML:    <span class="nav-icon">â—Ž</span>
                fix: <span class="nav-icon">&#x25CE;</span>

      Lines ~1409,1413,1417,1422 — desktop sidebar nav icons (duplicates of 1386-1398):
                Apply the same entity fixes as the mobile nav icons above.

      Lines ~1464,1468,1472,1476,1480,1484,1488,1492 HTML board-icon divs: "ðŸŸ "
                fix each: &#x1F7E0;

      Line ~1496 HTML EUC board-icon div: "ðŸ"˜"
                fix: &#x1F4D8;

      Line ~1558 HTML:    <span id="live-temp">--Â°</span>
                fix: <span id="live-temp">--&#x00B0;</span>

      Line ~1566 HTML:    <span style="font-size:0.9rem;">ðŸ"¡</span>
                fix: <span style="font-size:0.9rem;">&#x1F4E1;</span>

      Line ~1572 HTML:    <button class="btn-stop" ...>â¬› Stop &amp; Save</button>
                fix: <button class="btn-stop" ...>&#x2B1B; Stop &amp; Save</button>

      Line ~1660 HTML:    <button class="modal-close" ...>âœ•</button>
                fix: <button class="modal-close" ...>&#x2715;</button>

    CRITICAL RULES for this task:
    - Do NOT use fenced code blocks in this action — the above table IS the directive.
    - Replace EVERY garbled sequence. After applying the table, re-run the scan. If any
      non-ASCII lines remain that are not in the table, apply the mapping rules:
        * â†' (E2 86 92) → → or &rarr; depending on context
        * â€" (E2 80 94) → — or &mdash; depending on context
        * â€¦ (E2 80 A6) → … or &hellip; depending on context
        * Â° (C2 B0)    → ° or &deg; or &#x00B0; depending on context
        * Â· (C2 B7)    → · or &middot; depending on context
        * Multi-byte sequences that decode as emoji → use &#xXXXX; HTML entity in HTML
          context, or \uXXXX / 🌙 surrogate pair in JS string context
    - After every replacement, do NOT re-read the whole file — use Grep to confirm
      specific lines changed correctly, then move on.
    - Do NOT touch any other content in index.html (no reformatting, no logic changes).
  </action>

  <verify>
    <automated>
      powershell -Command "Select-String -Path C:/Users/bills/ShastaCoFloat/index.html -Pattern '[^\x00-\x7F]'"
    </automated>
    Expected output: no lines returned (zero matches).
    If any lines are returned, apply the remaining fixes before proceeding to Task 2.
  </verify>

  <acceptance_criteria>
    - PowerShell scan returns ZERO lines for pattern '[^\x00-\x7F]'
    - Line ~588 contains '°' (not 'Â°') — degree sign in JS
    - Line ~587 contains '°' (not 'Â°') — degree sign in template literal
    - Line ~630 contains '…' (not 'â€¦') — ellipsis in Connecting label
    - Line ~1558 contains '&#x00B0;' (not 'Â°') — degree in live-temp span
    - Board picker divs contain '&#x1F7E0;' (not 'ðŸŸ ') for Onewheel models
    - Board picker EUC div contains '&#x1F4D8;' (not 'ðŸ"˜')
    - No functional logic in index.html has been altered (BLE guard from Phase 1 intact)
  </acceptance_criteria>

  <done>Select-String returns zero non-ASCII matches and the file otherwise unchanged from its Phase 1 state.</done>
</task>

<task type="auto">
  <name>Task 2: Bump sw.js cache version and push</name>

  <read_first>
    - C:/Users/bills/ShastaCoFloat/sw.js (read in full — confirm current version string)
    - C:/Users/bills/ShastaCoFloat/CLAUDE.md (bump-SW, commit, and push convention)
  </read_first>

  <files>sw.js</files>

  <action>
    1. Read sw.js. Confirm line 1 currently reads:
         const CACHE = 'ridelogger-v21';
       If it reads something else, use whatever version is there and increment by 1.

    2. Change the CACHE constant to:
         const CACHE = 'ridelogger-v22';

    3. Run the post-fix verification scan on index.html one final time (must still be clean).

    4. Stage both files and commit:
         git add index.html sw.js
         git commit -m "fix(02): replace all non-ASCII bytes; bump SW to ridelogger-v22"

    5. Push to origin:
         git push origin main

    6. Confirm the push succeeded (no error output from git push).
  </action>

  <verify>
    <automated>
      powershell -Command "Select-String -Path C:/Users/bills/ShastaCoFloat/sw.js -Pattern 'ridelogger-v22'"
    </automated>
    Expected output: one line showing `const CACHE = 'ridelogger-v22';`

    Also verify the clean scan still holds:
    <automated>
      powershell -Command "Select-String -Path C:/Users/bills/ShastaCoFloat/index.html -Pattern '[^\x00-\x7F]'"
    </automated>
    Expected output: zero lines.

    Also verify git push completed:
    <automated>git log --oneline -1</automated>
    Expected: commit message contains "ridelogger-v22"
  </verify>

  <acceptance_criteria>
    - sw.js line 1 is exactly: const CACHE = 'ridelogger-v22';
    - index.html non-ASCII scan still returns zero matches
    - git log shows the Phase 2 fix commit as HEAD on main
    - git push exited with code 0 (no error)
  </acceptance_criteria>

  <done>sw.js version is ridelogger-v22, commit is on origin/main, and the non-ASCII scan is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source file → browser | index.html is served over HTTPS; content integrity depends on the service worker cache |
| service worker → cached response | SW serves cached index.html; version bump forces cache invalidation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | index.html character replacement | mitigate | Replacement is purely cosmetic (display strings and code comments); no logic paths altered. PowerShell scan post-edit confirms no residual non-ASCII bytes. |
| T-02-02 | Denial of Service | Partial encoding fix | mitigate | Scan gate in Task 1 verify block enforces zero-match before proceeding to commit; partial fixes cannot ship. |
| T-02-03 | Elevation of Privilege | SW cache version skipped | accept | Cache version is a monotonic string; skipping a version number has no security impact — users still receive the new build. |
| T-02-04 | Information Disclosure | Firestore API key in index.html | accept | API key is intentionally client-side (Firebase web SDK pattern). Firestore security rules enforce auth-gated access. No change in this phase. |
</threat_model>

<verification>
Run after both tasks complete:

1. Non-ASCII scan must be clean:
   powershell -Command "Select-String -Path C:/Users/bills/ShastaCoFloat/index.html -Pattern '[^\x00-\x7F]'"
   Expected: no output.

2. SW version confirmed:
   powershell -Command "Select-String -Path C:/Users/bills/ShastaCoFloat/sw.js -Pattern 'ridelogger-v22'"
   Expected: one match on line 1.

3. Commit on remote:
   git log --oneline origin/main -1
   Expected: Phase 2 fix commit is HEAD.

Manual spot-check (open app in browser after push):
- Live ride screen: temperature placeholder shows "--°" with degree symbol
- Board picker: orange circle emoji appears for Onewheel board cards
- Nav bar: lightning bolt, record dot, pencil, profile circle icons all render correctly
- Ride list: date separator shows "·" (middle dot) between date and time
</verification>

<success_criteria>
1. PowerShell scan `Select-String -Path index.html -Pattern '[^\x00-\x7F]'` returns zero matches.
2. sw.js contains `ridelogger-v22`.
3. Commit pushed to origin/main with both files.
4. No functional code was altered — BLE auth guard from Phase 1 is intact at ~line 392.
</success_criteria>

<output>
After completion, create .planning/phases/02-encoding-fix-deploy/02-01-SUMMARY.md following
the summary template at C:/Users/bills/.claude/get-shit-done/templates/summary.md.

Include in the summary:
- requirements-completed: [ENC-01, ENC-02, ENC-03, DEPLOY-01]
- key-files modified: index.html, sw.js
- The final SW cache version (ridelogger-v22)
- Confirmation that the non-ASCII scan returned zero matches
</output>
