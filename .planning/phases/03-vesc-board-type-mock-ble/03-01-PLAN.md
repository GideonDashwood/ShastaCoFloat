---
phase: 03-vesc-board-type-mock-ble
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - sw.js
autonomous: true
requirements:
  - VESC-01
  - VESC-02
  - VESC-03
  - VESC-05

must_haves:
  truths:
    - "VESC appears in the board picker grid and can be clicked/selected (highlighted)"
    - "After selecting VESC, a 'Connect VESC (Mock)' button appears below the board grid"
    - "Clicking 'Connect VESC (Mock)' starts the mock interval; bleConnected becomes true; toast 'VESC mock connected' appears"
    - "After starting a ride with VESC selected, the #vesc-stats-row is visible and all four cells (battery, duty, temp, voltage) update every 2 seconds with non-placeholder values"
    - "The mock interval is registered in vescMockInterval and is cleared when clearBlePollers() is called"
    - "The connectVESCMock function body contains the stub comment block with NUS UUIDs and COMM_GET_VALUES packet"
    - "SW cache version is bumped from ridelogger-v28 to ridelogger-v29 in sw.js after all index.html edits"
  artifacts:
    - path: "index.html"
      provides: "VESC board button, vesc-connect-row div, vesc-stats-row div, connectVESCMock function, updated selectBoard/startRecording/clearBlePollers"
      contains: "connectVESCMock"
    - path: "sw.js"
      provides: "Bumped cache version"
      contains: "ridelogger-v29"
  key_links:
    - from: "board-card[data-board='vesc-VESC']"
      to: "selectBoard('vesc','VESC')"
      via: "onclick"
      pattern: "selectBoard\\('vesc','VESC'\\)"
    - from: "connectVESCMock()"
      to: "vescMockInterval"
      via: "setInterval"
      pattern: "vescMockInterval\\s*="
    - from: "startRecording()"
      to: "#vesc-stats-row"
      via: "style.display"
      pattern: "vesc-stats-row"
---

<objective>
Add VESC as a selectable board type (board picker button + connect row) and implement the mock BLE
connection with live telemetry display. After this plan executes, the user can pick VESC, click
"Connect VESC (Mock)", start a ride, and see all five telemetry values updating on the ride screen.
The SW cache version is bumped and changes are committed so the next plan starts from a clean state.

Purpose: Delivers the live-telemetry half of the VESC vertical slice so the feature is immediately
visible and testable end-to-end before the save/persistence work in Plan 02.

Output: Modified index.html with VESC button, connect row, stats row, connectVESCMock() function,
updated selectBoard/startRecording/clearBlePollers, and new state variables. Modified sw.js bumped
to ridelogger-v29. Both files committed together.
</objective>

<execution_context>
@C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/.planning/phases/03-vesc-board-type-mock-ble/03-RESEARCH.md
</execution_context>

<context>
@C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/.planning/ROADMAP.md
@C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/CLAUDE.md

<interfaces>
<!-- Key state variables and functions the executor will reference/modify. Verified line numbers from index.html. -->

State variables (lines 54-64):
  let bleConnected = false;
  let boardSpeedMph = null;
  let boardBattery = null;
  let boardTempF = null;
  let boardDutyCycle = null;
  let blePollers = [];
  let bleKeepAlive = null;

New variables to insert after bleKeepAlive (line ~64):
  let vescMockInterval = null;
  let vescVoltage = null;

clearBlePollers() (line 683):
  function clearBlePollers() {
    blePollers.forEach(clearTimeout);
    blePollers = [];
    if (bleKeepAlive) { clearInterval(bleKeepAlive); bleKeepAlive = null; }
  }
  -- Must add: if (vescMockInterval) { clearInterval(vescMockInterval); vescMockInterval = null; }

selectBoard() (line 734):
  window.selectBoard = function(type, model) {
    selectedBoard = { type, model };
    document.querySelectorAll('.board-card').forEach(c => c.classList.remove('selected'));
    document.querySelector(`[data-board="${type}-${model}"]`).classList.add('selected');
    document.getElementById('start-btn').disabled = false;
    document.getElementById('gps-status').textContent = 'GPS ready -- tap Start Ride';
    const bleRow = document.getElementById('ble-row');
    if (bleRow) bleRow.style.display = type === 'onewheel' ? 'block' : 'none';
  }
  -- Must add two lines after the bleRow block:
    const vescRow = document.getElementById('vesc-connect-row');
    if (vescRow) vescRow.style.display = type === 'vesc' ? 'block' : 'none';

startRecording() (line 757):
  document.getElementById('ble-stats-row').style.display =
    selectedBoard.type === 'onewheel' ? 'grid' : 'none';
  -- Must add after that line:
    document.getElementById('vesc-stats-row').style.display =
      selectedBoard.type === 'vesc' ? 'grid' : 'none';

setBleStatus(state, name) (line 714) -- reuse as-is, no changes needed.

toast(msg) (line 993) -- reuse as-is.

Board picker HTML location (lines 1556-1593):
  <div class="board-grid">
    ... existing board-card buttons ...
    <button class="board-card" onclick="selectBoard('euc','')" data-board="euc-">  <!-- last button -->
      <div class="board-icon">&#x1F4D8;</div>
      <div>EUC</div>
    </button>
  </div>   <!-- closing board-grid div -->

BLE row HTML location (lines 1597-1603) -- insert #vesc-connect-row immediately after:
  <div id="ble-row">
    <button class="btn-ble" id="ble-btn" onclick="connectBLE()">...Connect Onewheel...</button>
    <div id="ble-diag" ...></div>
  </div>

Active-ride section (lines 1646-1663):
  <!-- BLE telemetry row -- only shown for Onewheel -->
  <div class="live-stats-secondary" id="ble-stats-row" style="display:none;margin-top:0;">
    ... four live-stat-sm divs ...
  </div>
  -- Insert #vesc-stats-row immediately after the closing </div> of ble-stats-row (before record-map div)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add VESC board button, connect row HTML, and state variables</name>
  <files>index.html</files>
  <read_first>
    Read index.html fully before touching anything. Key sections to locate precisely:
    - Line ~64: bleKeepAlive declaration (insert new vars after this line)
    - Lines 1556-1593: board-grid div (insert VESC button after EUC button, before closing board-grid div)
    - Lines 1597-1603: ble-row div (insert vesc-connect-row after closing ble-row div)
    - Lines 1646-1663: ble-stats-row div (insert vesc-stats-row after closing ble-stats-row div)
    - Line 683: clearBlePollers function (add vescMockInterval clear)
    - Line 734: selectBoard function (add vesc-connect-row show/hide)
    - Line 757: startRecording function (add vesc-stats-row show/hide)
  </read_first>
  <action>
    Make seven surgical edits to index.html:

    EDIT 1 — State variables (after line 64 "let bleKeepAlive = null;"):
    Insert two new lines:
      let vescMockInterval = null;   // handle for VESC mock setInterval
      let vescVoltage = null;        // current VESC voltage reading

    EDIT 2 — clearBlePollers() (line ~683, inside the function body, after the bleKeepAlive block):
    Add one line at the end of the function body before the closing brace:
      if (vescMockInterval) { clearInterval(vescMockInterval); vescMockInterval = null; }

    EDIT 3 — selectBoard() (line ~734, at the end of the function body before the closing brace):
    Add two lines after "if (bleRow) bleRow.style.display = type === 'onewheel' ? 'block' : 'none';":
      const vescRow = document.getElementById('vesc-connect-row');
      if (vescRow) vescRow.style.display = type === 'vesc' ? 'block' : 'none';

    EDIT 4 — startRecording() (line ~757, after the ble-stats-row display line):
    After "document.getElementById('ble-stats-row').style.display = selectedBoard.type === 'onewheel' ? 'grid' : 'none';"
    Add one line:
      document.getElementById('vesc-stats-row').style.display = selectedBoard.type === 'vesc' ? 'grid' : 'none';

    EDIT 5 — Board picker HTML (after the closing </button> of the EUC button at line ~1592, before </div> that closes board-grid):
    Insert:
      <button class="board-card" onclick="selectBoard('vesc','VESC')" data-board="vesc-VESC">
        <div class="board-icon">&#x26A1;</div>
        <div>VESC</div>
      </button>

    EDIT 6 — After the closing </div> of #ble-row (line ~1603), insert the vesc-connect-row:
      <!-- VESC mock connect -- only shown when VESC is selected -->
      <div id="vesc-connect-row" style="display:none;margin-top:0.75rem;">
        <button class="btn-ble" id="vesc-btn" onclick="connectVESCMock()">
          <span class="ble-dot" id="vesc-dot"></span>
          Connect VESC (Mock)
        </button>
      </div>

    EDIT 7 — After the closing </div> of #ble-stats-row (line ~1663), before the record-map div, insert the vesc-stats-row:
      <!-- VESC telemetry row -- only shown for VESC -->
      <div class="live-stats-secondary" id="vesc-stats-row" style="display:none;margin-top:0;">
        <div class="live-stat-sm">
          <span id="vesc-live-battery">--%</span>
          <label>battery</label>
        </div>
        <div class="live-stat-sm">
          <span id="vesc-live-duty">--%</span>
          <label>duty cycle</label>
        </div>
        <div class="live-stat-sm">
          <span id="vesc-live-temp">--&#x00B0;</span>
          <label>motor temp</label>
        </div>
        <div class="live-stat-sm">
          <span id="live-voltage">--V</span>
          <label>voltage</label>
        </div>
      </div>

    Do NOT alter any existing BLE code, OW telemetry handlers, or other board logic.
    Do NOT add fenced code blocks inside this action — edit only the actual file.
  </action>
  <verify>
    Open index.html in a browser (or use PowerShell):
      Select-String -Path "C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/index.html" -Pattern "vesc-connect-row|vesc-stats-row|vescMockInterval|data-board=.vesc-VESC"
    All four patterns must match. Expected: at least one match each.
  </verify>
  <acceptance_criteria>
    - index.html contains a button with data-board="vesc-VESC" and onclick="selectBoard('vesc','VESC')"
    - index.html contains div id="vesc-connect-row" with display:none initial style
    - index.html contains div id="vesc-stats-row" with display:none initial style and four child live-stat-sm divs (vesc-live-battery, vesc-live-duty, vesc-live-temp, live-voltage)
    - let vescMockInterval = null; and let vescVoltage = null; are declared in the state section
    - clearBlePollers() body contains clearInterval(vescMockInterval) guard
    - selectBoard() body shows/hides vesc-connect-row based on type === 'vesc'
    - startRecording() body shows vesc-stats-row as 'grid' when type === 'vesc'
  </acceptance_criteria>
  <done>All seven edits applied; grep confirms all four key patterns present in index.html.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement connectVESCMock() function with stub comment block</name>
  <files>index.html</files>
  <read_first>
    Read index.html around line 683 (clearBlePollers) and line 734 (selectBoard) to find the
    correct insertion point. The connectVESCMock function should be inserted in the
    "// ============ RECORD ============" section, immediately before the selectBoard function
    (which starts at line ~734). This keeps all VESC-related record logic together.
  </read_first>
  <action>
    Insert the connectVESCMock function into index.html immediately before
    "window.selectBoard = function(type, model) {" (line ~734).

    The complete function to insert (per D-01 decision and RESEARCH.md mock telemetry ranges):

    window.connectVESCMock = function() {
      // TODO: VESC BLE -- replace this mock with real VESC protocol
      // Real implementation: connect to Nordic UART Service (NUS)
      //   Service UUID : 6e400001-b5a3-f393-e0a9-e50e24dcca9e
      //   RX char (write): 6e400002-b5a3-f393-e0a9-e50e24dcca9e
      //   TX char (notify): 6e400003-b5a3-f393-e0a9-e50e24dcca9e
      // Send COMM_GET_VALUES request: [0x02, 0x01, 0x04, CRC_HI, CRC_LO, 0x03]
      // Parse response (big-endian, byte offsets from payload start):
      //   [0]      : command echo (0x04)
      //   [1-2]    : temp_fet * 10  (Int16) -> divide by 10 for deg C
      //   [3-4]    : temp_motor * 10 (Int16) -> divide by 10 for deg C
      //   [21-22]  : duty_cycle * 1000 (Int16) -> divide by 10 for %
      //   [23-26]  : rpm (Int32) -> convert to mph via wheel circumference
      //   [27-28]  : v_in * 10 (Int16) -> divide by 10 for volts
      // All multi-byte values are big-endian.
      bleConnected = true;
      setBleStatus('connected', 'VESC Mock');
      toast('VESC mock connected');
      let t = 0;
      vescMockInterval = setInterval(() => {
        t += 0.1;
        const battery  = Math.round(60 + 30 * Math.sin(t * 0.05));
        const duty     = Math.round(20 + 40 * Math.abs(Math.sin(t * 0.3)));
        const tempC    = +(35 + 15 * Math.sin(t * 0.07)).toFixed(1);
        const speedMph = +(10 + 12 * Math.abs(Math.sin(t * 0.2))).toFixed(1);
        const voltage  = +(48 + 8 * Math.sin(t * 0.04)).toFixed(1);
        boardBattery   = battery;
        boardDutyCycle = duty;
        boardTempF     = tempC * 9 / 5 + 32;
        boardSpeedMph  = speedMph;
        vescVoltage    = voltage;
        if (speedMph > topSpeedMph) topSpeedMph = speedMph;
        document.getElementById('vesc-live-battery').textContent = battery + '%';
        document.getElementById('vesc-live-duty').textContent    = duty + '%';
        document.getElementById('vesc-live-temp').textContent    = tempC.toFixed(0) + '°';
        document.getElementById('live-voltage').textContent      = voltage + 'V';
        document.getElementById('live-speed').textContent        = speedMph.toFixed(1);
        document.getElementById('live-topspeed').textContent     = topSpeedMph.toFixed(1);
      }, 2000);
    }

    Notes on the implementation:
    - Writes to vesc-live-battery, vesc-live-duty, vesc-live-temp (in vesc-stats-row) and live-voltage
    - Also writes to live-speed and live-topspeed (primary always-visible row) per research recommendation
    - Sets boardSpeedMph so onGpsUpdate() GPS fallback is suppressed (bleConnected=true already handles this,
      but setting boardSpeedMph to a non-null value ensures the primary speed card updates from mock data)
    - Uses setBleStatus() and toast() — no new UI helpers needed
    - The stub comment block is inside the function body as the first block of lines
  </action>
  <verify>
    Select-String -Path "C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/index.html" -Pattern "connectVESCMock|TODO: VESC BLE|6e400001|vescMockInterval\s*="
    All four patterns must match.
  </verify>
  <acceptance_criteria>
    - connectVESCMock function exists in index.html and is assigned to window.connectVESCMock
    - The stub comment block inside the function body contains all three NUS UUIDs (6e400001, 6e400002, 6e400003) and references COMM_GET_VALUES
    - The function sets bleConnected = true, calls setBleStatus('connected', 'VESC Mock'), and calls toast('VESC mock connected')
    - vescMockInterval is assigned the return value of setInterval
    - The interval callback writes to all six DOM IDs: vesc-live-battery, vesc-live-duty, vesc-live-temp, live-voltage, live-speed, live-topspeed
    - The interval fires every 2000ms
    - boardBattery, boardDutyCycle, boardTempF, boardSpeedMph, and vescVoltage are all assigned in the callback
    - VESC-05 stub comment is present (// TODO: VESC BLE -- replace this mock with real VESC protocol)
  </acceptance_criteria>
  <done>connectVESCMock is in index.html; stub comment block is present with NUS UUIDs; all DOM writes confirmed; grep passes for all four patterns.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bump SW cache version and commit Plan 01 changes</name>
  <files>sw.js</files>
  <read_first>
    Read sw.js. Confirm current cache version is 'ridelogger-v28' on line 1.
    Confirm Tasks 1 and 2 edits to index.html are complete before touching sw.js.
  </read_first>
  <action>
    CLAUDE.md requires a SW version bump after every index.html change. Plan 01 makes seven edits
    to index.html, so sw.js must be bumped NOW — before Plan 02 runs — to avoid a stale-cache
    window between deploys.

    EDIT sw.js line 1:
    OLD: const CACHE = 'ridelogger-v28';
    NEW: const CACHE = 'ridelogger-v29';

    Then commit both files together:
      git add index.html sw.js
      git commit -m "feat(vesc): add VESC board button, mock BLE connect, and live telemetry display (phase 3 plan 01)"

    Do NOT push yet — Plan 02 will push after its own changes and second SW bump.
  </action>
  <verify>
    Select-String -Path "C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/sw.js" -Pattern "ridelogger-v29"
    Must return one match.

    git log --oneline -1
    Most recent commit message must reference VESC or phase 3.
  </verify>
  <acceptance_criteria>
    - sw.js line 1 reads: const CACHE = 'ridelogger-v29';
    - git log --oneline -1 shows a commit covering Plan 01 VESC changes
    - Both index.html and sw.js are included in that commit
  </acceptance_criteria>
  <done>sw.js updated to ridelogger-v29; both files committed; git log confirms the Plan 01 commit on feature/ridelogger-scaffold branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock interval -> DOM | setInterval callback writes values directly to DOM spans — no user input involved, no XSS surface |
| Firestore (handled in Plan 02) | User-supplied ride title/notes reach Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | vescMockInterval | mitigate | interval is always cleared in clearBlePollers(); stopRecording() must invoke clearBlePollers() — verified in Plan 02 |
| T-03-02 | Tampering | Mock telemetry values | accept | values are hardcoded sinusoids; no user-controlled input; no security boundary crossed |
</threat_model>

<verification>
After all three tasks complete, verify in browser:
1. Board picker shows VESC button (lightning bolt icon)
2. Clicking VESC highlights it and shows "Connect VESC (Mock)" button
3. Clicking "Connect VESC (Mock)" shows toast "VESC mock connected"; ble-dot turns green
4. Clicking "Start Ride" shows active-ride section with vesc-stats-row visible (battery/duty/temp/voltage)
5. Values update every ~2 seconds with realistic ranges
6. Primary speed card also updates from mock

PowerShell grep sanity check:
  Select-String -Path "C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/index.html" -Pattern "vesc-VESC|vesc-connect-row|vesc-stats-row|connectVESCMock|vescMockInterval|TODO: VESC BLE"
All six patterns must match.

  Select-String -Path "C:/Users/wsuhr/Projects/my-pwa/ShastaCoFloat/sw.js" -Pattern "ridelogger-v29"
Must return one match.
</verification>

<success_criteria>
- VESC button in board picker: DONE when data-board="vesc-VESC" button is in board-grid
- VESC connect row: DONE when #vesc-connect-row exists and shows only when VESC selected
- VESC stats row: DONE when #vesc-stats-row exists with four metric cells and shows only during active VESC ride
- Mock function: DONE when connectVESCMock() is defined, interval fires at 2s, all six DOM IDs updated
- Stub comment: DONE when NUS UUIDs and COMM_GET_VALUES are documented inside function body
- SW bump v28->v29: DONE when sw.js reads ridelogger-v29 and both files are committed
- No regressions: OW board selection, BLE connect flow, and EUC board selection unchanged
</success_criteria>

<output>
After completion, create .planning/phases/03-vesc-board-type-mock-ble/03-01-SUMMARY.md summarising
what was changed, exact line numbers of each edit, and any deviations from the plan.
</output>
